import {cn} from "@/lib/utils";
import {Button} from "@/components/ui/button";
const page = ()=>{
  return (
    <div className={cn("text-red-500 font-bold")}><Button>Click Me</Button></div>

  );
};

export default page;